# ─── Lab Assistant AI - Python Dependencies (Python 3.10+) ────────────────────
# Tested on Windows 10/11 with Python 3.10
# Install: pip install -r requirements.txt

# ── Django Core ───────────────────────────────────────────────────────────────
Django==4.2.16
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
channels==4.0.0
channels-redis==4.2.0
daphne==4.0.0

# ── Database Drivers ──────────────────────────────────────────────────────────
# SQL Server (already installed from your requirements)
pyodbc==5.1.0
# PostgreSQL (optional)
psycopg2-binary==2.9.9

# ── AI / LangChain Stack (FIXED - compatible versions) ───────────────────────
# langchain-community 0.3.7 requires langchain-core>=0.3.17
# langchain 0.3.7 requires langchain-core>=0.3.15
# Solution: pin langchain-core==0.3.21 which satisfies both
langchain==0.3.7
langchain-community==0.3.7
langchain-core==0.3.21

# ── LLM / Ollama ─────────────────────────────────────────────────────────────
ollama==0.3.3

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb==0.5.18
faiss-cpu==1.8.0

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers==3.2.1
# torch: use CPU build for Windows without GPU
# If you have NVIDIA GPU, replace with: torch==2.4.1+cu121
torch==2.4.1
transformers==4.46.2

# ── Document Processing ───────────────────────────────────────────────────────
pypdf==4.3.1
pymupdf==1.24.10
python-docx==1.1.2
python-pptx==1.0.2
docx2txt==0.8
# unstructured has many optional deps - install core only
unstructured==0.16.0

# ── REST / HTTP ───────────────────────────────────────────────────────────────
requests==2.32.3
httpx==0.27.2

# ── Security ──────────────────────────────────────────────────────────────────
cryptography==43.0.3
python-dotenv==1.0.1

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.9.2
python-dateutil==2.9.0
pillow==10.4.0
tiktoken==0.8.0
tqdm==4.66.6
numpy==1.26.4
pandas==2.2.3

# ── Development / Testing ─────────────────────────────────────────────────────
pytest==8.3.3
pytest-django==4.9.0
pytest-asyncio==0.24.0
factory-boy==3.3.1
coverage==7.6.4
black==24.10.0
flake8==7.1.1
